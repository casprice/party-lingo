import { IDataStore, IListeners } from './types';
export default class Listeners<T> implements IListeners<T> {
    listeners: any;
    addEventListener(type: T, callback: (data: any, datastore: IDataStore) => Promise<void>): void;
    removeEventListener(type: T, callback: (data: any, datastore: IDataStore) => Promise<void>): void;
    removeAllEventListeners(): void;
    runEventListeners(type: T, data: any, datastore: IDataStore): Promise<void>;
}
