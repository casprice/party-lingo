import Listeners from './listeners';
import { Client, IDataStore, Response, ServerEvents } from './types';
export declare class WebSocketGameLobbyServer extends Listeners<ServerEvents> {
    listeners: any;
    wss: any;
    datastore: IDataStore;
    constructor({ port, server, datastore }: {
        port: number;
        server: any;
        datastore: IDataStore;
    });
    sendUpdate(client: Client): Promise<Partial<Response>>;
    broadcastUpdate(gameId: string): Promise<void>;
}
